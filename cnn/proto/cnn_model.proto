syntax = "proto3";

package cnn_model;

service CnnModel {
  rpc ScoreCnnModel(QueryRequest) returns (QueryReply) {}
}

message QueryRequest {
  string query = 1;
}

message QueryReply {
  repeated string app = 1;
  repeated float prob = 2;
}
